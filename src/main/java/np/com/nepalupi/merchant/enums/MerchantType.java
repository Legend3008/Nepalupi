package np.com.nepalupi.merchant.enums;

public enum MerchantType {
    SMALL,   // Kirana store, tea stall — self-onboarded, zero MDR
    LARGE    // Business with API integration, settlement reporting
}
